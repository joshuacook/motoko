# API Keys
ANTHROPIC_API_KEY=sk-ant-your-key-here
GOOGLE_API_KEY=your-google-api-key-here

# Authentication
JWT_SECRET=$(openssl rand -hex 32)
PASSWORD_HASH=$(python -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt()).decode())")

# Paths
WORKSPACE_DIR=$(pwd)
DB_DIR=./data

# Git
GIT_USER_NAME=Motoko
GIT_USER_EMAIL=motoko@agent.local

# Domain (for production)
DOMAIN=motoko.yourdomain.com
CADDY_EMAIL=root@joshuacook.net
